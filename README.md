# github-user-activity-fetcher
A simple command line interface program to fetch a recent activity of a github user and display it in the terminal. Events of the last 30 days are shown and only push/create events are supported for now.

User the command:
npm run github-activity <username> 
